node_modules/
dist/
.env
.env.sandbox
.env.production
.env.*
.vercel/

.vercel

# Beta tester personal data and generated email drafts — never commit
scripts/testers.json
scripts/drafts/

# Claude Code local data (permissions, memory, sessions) — per-machine, not for sharing
.claude/
